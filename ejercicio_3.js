let a, b; // se declaran las variables a y b.
a = 3; b = 7; //se inicializan las variables anteriores con valores numéricos.
resultado = a * b; //se realiza un producto entre las variables anteirores
document.write(`Variable a contiene ${a} <br> ` );
document.write(`Variable b contiene ${b} <br> ` );
document.write(`El producto de a por b es ${resultado} <br> `);
document.write("Los puntos de interrupcion son fundamentales para programar");
//se muestra dentro del documento el valor de las variables y el resultado.
//el error dentro del código era que en la línea 6, la variable resultado estaba mal llamada.