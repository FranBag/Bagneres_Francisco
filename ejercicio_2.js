let A, B; //se declaran las variables A y B.
A = 9;
B = 7;
//se inicializan las variables A y B.
resultado = A + B;
//se declara la variable resultado, la cual es la suma de A y B.
alert (`La suma de A+B es: ${resultado}`);
alert (`La diferencia de A-B es: ${A-B}`);
alert (`La multiplicación de AxB es: ${A*B}`);
alert (`La división de A/B es: ${resultado=A/B}`);
//se muestran en forma de alerta el resultado de algunas operaciones